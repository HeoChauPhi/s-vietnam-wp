{% set sidebar_filter = {'template_type': template_type, 'default_tour_date': default_tour_date, 'tour_feature_term': tour_feature_term} %}

{% for key, sidebar in sidebar_widget %}
  {% if key != '_multiwidget' %}
    {{ function('acfwidget', 'sidebar_blocks', 'sidebar_widget-' ~ key, sidebar_filter) }}
  {% endif %}
{% endfor %}